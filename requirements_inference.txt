# ST-GCN Dance Pose Generation - Inference Requirements
# Install with: pip install -r requirements_inference.txt

# IMPORTANT: MediaPipe requires Python 3.8-3.12 (not 3.13 yet)
# If you have Python 3.13, please downgrade to Python 3.11 or 3.12

# Core dependencies
opencv-python-headless>=4.8.0
mediapipe>=0.10.0,<0.11.0
torch>=2.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Optional: For CUDA support (if available)
# torch with CUDA can be installed separately:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
